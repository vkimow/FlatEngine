cmake_minimum_required(VERSION 3.13 FATAL_ERROR)

project(Core CXX)

string(TOUPPER ${PROJECT_NAME} PROJECT_UPPER_NAME)

set(FLAT_CURRENT_INCLUDE_DIR "${FLAT_INCLUDE_DIR}/FlatEngine/${PROJECT_NAME}")
set(FLAT_CURRENT_SOURCE_DIR "${FLAT_SOURCE_DIR}/FlatEngine/${PROJECT_NAME}")

set(CURRENT_SOURCES "${PROJECT_UPPER_NAME}_SOURCES")
set(CURRENT_PUBLIC_HEADERS "${PROJECT_UPPER_NAME}_PUBLIC_HEADERS")

set(${CURRENT_SOURCES}
    "${FLAT_CURRENT_SOURCE_DIR}/App.cpp"
	)
	
set(${CURRENT_PUBLIC_HEADERS}
    "${FLAT_CURRENT_INCLUDE_DIR}/App.h"
	)

set(CURRENT_SOURCES ${${PROJECT_UPPER_NAME}_SOURCES})
set(CURRENT_PUBLIC_HEADERS ${${PROJECT_UPPER_NAME}_PUBLIC_HEADERS})

add_flat_library(LIBRARY_NAME ${PROJECT_NAME} SOURCES ${CURRENT_SOURCES} PUBLIC_HEADERS ${CURRENT_PUBLIC_HEADERS})